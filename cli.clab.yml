name: cli
prefix: ""

topology:
  nodes:
    srl:
      kind: nokia_srlinux
      image: ghcr.io/nokia/srlinux:24.10.2
      binds:
        # the uptime.py implements the `show uptime` command
        - uptime/uptime.py:/etc/opt/srlinux/cli/plugins/uptime/uptime.py
